name: Tests
on: [push, pull_request]
jobs:
  cache:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python: ['3.10', '3.12']
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - run: python -m pip install triton==3.8.0 '.[test]'
      - run: python -m pytest -s --tb=short tests
